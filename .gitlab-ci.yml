variables:
  CPRAN_GIT: https://gitlab.com/cpran/plugin_

before_script:
  - pushd ..
  - rm -rf plugin_{utils,tap}
  - git clone ${CPRAN_GIT}tap.git
  - git clone ${CPRAN_GIT}utils.git
  - popd

Praat 5.4.22:
  script:
    - prove -v --exec "praat-5.4.22 --ansi --pref-dir=../.."
  tags:
    - cpran
  allow_failure: true

Praat 6.0.03:
  script:
    - prove -v --exec "praat-6.0.03 --ansi --pref-dir=../.. --run"
  tags:
    - cpran
  allow_failure: true

Praat 6.0.17:
  script:
    - prove -v --exec "praat-6.0.17 --ansi --pref-dir=../.. --run"
  tags:
    - cpran
  allow_failure: true

Praat 6.0.18:
  script:
    - prove -v --exec "praat-6.0.18 --ansi --pref-dir=../.. --run"
  tags:
    - cpran
  allow_failure: true

Praat 6.0.19:
  script:
    - prove -v --exec "praat-6.0.19 --ansi --pref-dir=../.. --run"
  tags:
    - cpran
